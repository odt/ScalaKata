import org.scalatest.FunSpec
import org.scalatest.matchers.ShouldMatchers

class MungingDataSpec extends FunSpec with ShouldMatchers {
  // describe("SortingBalls#add, balls") {
  //   it ("should be empty") {
  //     Rack[Int]().balls should equal(List())
  //   }
  // }
}
